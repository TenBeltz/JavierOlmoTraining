--- 
const { number, text, isLastStep } = Astro.props;
---

<div class="flex items-center md:w-48 gap-x-2 md:flex-col">
  <div class={`relative z-10 flex items-center justify-center min-w-20 min-h-20 text-5xl font-bold text-white rounded-full ${isLastStep ? 'bg-lime-zest' : 'bg-black'}`}>
    <span>{number}</span>
    {!isLastStep && (
      <span class="absolute hidden md:block -z-10 bg-black border-2 border-black top-10 w-[20vw] left-10"></span>
    )}
  </div>
  <p>{text}</p>
</div>